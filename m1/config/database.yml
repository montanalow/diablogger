defaults: &defaults
  adapter: sqlite3

development:
  database: diablogger_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgresql
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: diablogger_test.db
  <<: *defaults
production:
  database: diablogger_production.db
  <<: *defaults
